<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MarjamPrism.Views.MainPage"
             xmlns:b="clr-namespace:Prism.Behaviors;assembly=Prism.Forms">
    

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="FillAndExpand" >
        
        <ListView x:Name="ItemsListView"
                Margin="10, 0, 10, 0"
                ItemsSource= "{Binding Products}"
                VerticalOptions="FillAndExpand"
                HasUnevenRows="true"
                HorizontalOptions="FillAndExpand"
                SelectedItem="{Binding SelectedProduct}"
                SeparatorVisibility="None">
            <ListView.Behaviors>
                <b:EventToCommandBehavior EventName="ItemTapped" 
                                      Command="{Binding ItemSelectedCommand}"/>
            </ListView.Behaviors>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout BackgroundColor="White">
                            <Grid BackgroundColor="White"  Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="60"></RowDefinition>
                                    <RowDefinition Height="20"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                    <ColumnDefinition Width="20"></ColumnDefinition>
                                </Grid.ColumnDefinitions>


                                <!--<material:MaterialCard CornerRadius="2" Elevation="1" HeightRequest="80" HorizontalOptions="FillAndExpand" />-->


                                <Image x:Name="backgroundImage" Source="{Binding Image}"  Grid.RowSpan="2" Grid.Column='0' Grid.Row="0" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" Margin="8"/>
                                
                                <Label Text="{Binding Name}" 
                                LineBreakMode="WordWrap"
                                Style="{DynamicResource ListItemTextStyle}" 
                                FontSize="16"
                                TextColor="Black"
                                Margin="0, 18, 0, 0"
                                Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="2"/>
                                
                                <Label Text="{Binding RegularPrice}"
                                LineBreakMode="NoWrap" 
                                Style="{DynamicResource ListItemTextStyle}" 
                                FontSize="16"
                                TextColor="Black"
                                Grid.Column="1" Grid.Row="1" Margin="2,2,0,0"/>


                                <!--{Binding (InStoreAvailability) ? ('In Stock') : ('Out Of Stock'))}   This doesn't work when trying to show conditional statement-->
                                <Label Text="{Binding InStoreAvailability}"
                                LineBreakMode="NoWrap" 
                                Style="{DynamicResource ListItemTextStyle}" 
                                FontSize="16"
                                TextColor="Black"
                                Grid.Column="2" Grid.Row="1"/>

                                <Image Source="https://img.icons8.com/ios-glyphs/30/000000/chevron-right.png"
                                   x:Name="arrow"
                                   Grid.Column="3" Grid.Row="0" Grid.RowSpan="2"
                                   HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"
                                />

                            </Grid>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
    
    

</ContentPage>